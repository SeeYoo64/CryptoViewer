<UserControl x:Class="CryptoViewer.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:CryptoViewer.Converters"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
        <converters:StringResourceConverter x:Key="StringResourceConverter" x:Shared="False" />
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="{Binding Title}" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="0,0,0,10" />

        <!-- Navigation buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="{Binding Converter={StaticResource StringResourceConverter}, ConverterParameter=SearchCoinsButton}" Command="{Binding NavigateToSearchCommand}" 
                    Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,10,0" Width="120" />
            <Button Content="{Binding Converter={StaticResource StringResourceConverter}, ConverterParameter=ConvertCurrencyButton}" Command="{Binding NavigateToConverterCommand}" 
                    Style="{StaticResource MaterialDesignRaisedButton}" Width="180" />
            <ComboBox Grid.Row="0" ItemsSource="{Binding SupportedLanguages}" 
                  SelectedItem="{Binding SelectedLanguage, UpdateSourceTrigger=PropertyChanged}" 
                  Width="100" HorizontalAlignment="Right" Margin="40,0,0,0" />
        </StackPanel>
        <!-- Error message -->
        <TextBlock Grid.Row="1" Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,0,0,10" Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}" />

        <!-- Spreadsheet of coins -->
        <materialDesign:Card Grid.Row="3" Padding="10">
            <DataGrid x:Name="CoinsDataGrid" ItemsSource="{Binding Coins}" AutoGenerateColumns="False" IsReadOnly="True" 
                      CanUserAddRows="False">
                <DataGrid.Resources>
                    <converters:BindingProxy x:Key="proxy" Data="{Binding DataContext, ElementName=CoinsDataGrid}" />
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{Binding Data, Source={StaticResource proxy}, Converter={StaticResource StringResourceConverter}, ConverterParameter=RankHeader}" Binding="{Binding MarketCapRank}" Width="Auto" />
                    <DataGridTextColumn Header="{Binding Data, Source={StaticResource proxy}, Converter={StaticResource StringResourceConverter}, ConverterParameter=NameHeader}" Binding="{Binding Name}" Width="*" />
                    <DataGridTextColumn Header="{Binding Data, Source={StaticResource proxy}, Converter={StaticResource StringResourceConverter}, ConverterParameter=SymbolHeader}" Binding="{Binding Symbol}" Width="Auto" />
                    <DataGridTextColumn Header="{Binding Data, Source={StaticResource proxy}, Converter={StaticResource StringResourceConverter}, ConverterParameter=PriceHeader}" Binding="{Binding CurrentPrice, StringFormat={}{0:C2}}" Width="Auto" />
                    <DataGridTextColumn Header="{Binding Data, Source={StaticResource proxy}, Converter={StaticResource StringResourceConverter}, ConverterParameter=Change24hHeader}" Binding="{Binding PriceChangePercentage24h, StringFormat={}{0:N2}%}" Width="Auto" />
                    <DataGridTextColumn Header="{Binding Data, Source={StaticResource proxy}, Converter={StaticResource StringResourceConverter}, ConverterParameter=VolumeHeader}" Binding="{Binding TotalVolume, StringFormat={}{0:C0}}" Width="Auto" />
                </DataGrid.Columns>
                <DataGrid.InputBindings>
                    <MouseBinding Gesture="LeftDoubleClick" Command="{Binding NavigateToDetailsCommand}" 
                                  CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=DataGrid}}" />
                </DataGrid.InputBindings>
            </DataGrid>
        </materialDesign:Card>
    </Grid>
</UserControl>
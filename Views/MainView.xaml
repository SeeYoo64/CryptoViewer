<UserControl x:Class="CryptoViewer.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid Background="{DynamicResource MaterialDesignPaper}">
        <materialDesign:Card Padding="20" Margin="10" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Welcome to CryptoViewer!" 
                           FontSize="24" 
                           FontWeight="Bold"
                           Style="{StaticResource MaterialDesignHeadline5TextBlock}" 
                           HorizontalAlignment="Center" />
                <materialDesign:PackIcon Kind="Bitcoin" Width="24" Height="24" HorizontalAlignment="Center" Margin="0,10,0,0" />
                <Button Content="Test Button" 
                        Style="{StaticResource MaterialDesignRaisedButton}" 
                        Margin="10" 
                        Width="150" 
                        Click="Button_Click" />
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>